---
// index.astro — Landing Page
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Keto Journey — Transform Your Body in 30 Days</title>
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,700;0,9..144,900;1,9..144,700;1,9..144,900&family=DM+Sans:opsz,wght@9..40,300;9..40,400;9..40,500;9..40,600;9..40,700&display=swap" rel="stylesheet"/>
  <style>
    :root {
      --green:#10b981; --green2:#34d399; --gold:#f59e0b;
      --dark:#0a0f0a; --dark2:#111811; --card:#141f14;
      --border:rgba(16,185,129,.15); --text:#e8f0e8; --soft:#7a9a7a;
    }
    *,*::before,*::after { margin:0; padding:0; box-sizing:border-box; }
    html { scroll-behavior:smooth; }
    body {
      font-family:'DM Sans',sans-serif;
      background:var(--dark); color:var(--text);
      min-height:100vh; overflow-x:hidden;
    }

    /* ── FIXED BG ── */
    .bg-fixed {
      position:fixed; inset:0; z-index:0;
      background:
        radial-gradient(ellipse 80% 50% at 50% -10%, rgba(16,185,129,.12), transparent),
        radial-gradient(ellipse 60% 40% at 80% 80%,  rgba(245,158,11,.06), transparent),
        var(--dark);
    }
    .grain {
      position:fixed; inset:0; z-index:1; pointer-events:none; opacity:.025;
      background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    }

    /* ── NAV ── */
    .nav {
      position:fixed; top:0; left:0; right:0; z-index:100;
      padding:1.25rem 2.5rem;
      display:flex; align-items:center; justify-content:space-between;
      background:rgba(10,15,10,.7); backdrop-filter:blur(20px);
      border-bottom:1px solid var(--border);
    }
    .nav-logo {
      display:flex; align-items:center; gap:.6rem;
      font-family:'Fraunces',serif; font-weight:700; font-size:1.15rem;
      color:var(--text); text-decoration:none;
    }
    .nav-logo span { color:var(--green); }
    .nav-links { display:flex; align-items:center; gap:.25rem; }
    .nav-link {
      padding:.45rem 1rem; border-radius:99px;
      font-size:.82rem; font-weight:600; color:var(--soft);
      text-decoration:none; transition:all .2s;
    }
    .nav-link:hover { color:var(--text); }
    .nav-cta {
      display:flex; align-items:center; gap:.75rem;
    }
    .btn-ghost {
      padding:.55rem 1.2rem; border-radius:10px;
      font-size:.82rem; font-weight:700; color:var(--soft);
      text-decoration:none; border:1px solid rgba(255,255,255,.1);
      transition:all .22s;
    }
    .btn-ghost:hover { border-color:var(--green); color:var(--green); }
    .btn-green {
      padding:.55rem 1.4rem; border-radius:10px;
      font-size:.82rem; font-weight:800; color:#fff;
      text-decoration:none;
      background:linear-gradient(135deg,var(--green),var(--green2));
      box-shadow:0 4px 16px rgba(16,185,129,.3);
      transition:all .22s;
    }
    .btn-green:hover { transform:translateY(-2px); box-shadow:0 8px 24px rgba(16,185,129,.4); }

    /* ── HERO ── */
    .hero {
      position:relative; z-index:2;
      min-height:100vh;
      display:grid; grid-template-columns:1fr 1fr;
      gap:0; align-items:center;
    }
    .hero-left {
      padding:8rem 3rem 4rem 5rem;
    }
    .hero-eyebrow {
      display:inline-flex; align-items:center; gap:.5rem;
      background:rgba(16,185,129,.1); border:1px solid rgba(16,185,129,.25);
      border-radius:99px; padding:.38rem 1rem;
      font-size:.73rem; font-weight:800; color:var(--green);
      text-transform:uppercase; letter-spacing:.09em;
      margin-bottom:1.5rem; animation:fadeUp .6s ease both;
    }
    .hero-title {
      font-family:'Fraunces',serif;
      font-size:clamp(2.8rem,5vw,4.5rem);
      font-weight:900; line-height:1.06;
      margin-bottom:1.5rem;
      animation:fadeUp .6s .1s ease both;
    }
    .hero-title em {
      font-style:italic;
      background:linear-gradient(135deg,var(--green),var(--green2));
      -webkit-background-clip:text; -webkit-text-fill-color:transparent;
    }
    .hero-title .gold {
      font-style:italic;
      background:linear-gradient(135deg,var(--gold),#fbbf24);
      -webkit-background-clip:text; -webkit-text-fill-color:transparent;
    }
    .hero-sub {
      font-size:1.05rem; color:var(--soft); line-height:1.7;
      max-width:440px; margin-bottom:2.5rem;
      animation:fadeUp .6s .2s ease both;
    }
    .hero-btns {
      display:flex; align-items:center; gap:1rem; flex-wrap:wrap;
      margin-bottom:2.5rem;
      animation:fadeUp .6s .3s ease both;
    }
    .btn-hero {
      display:inline-flex; align-items:center; gap:.6rem;
      padding:.9rem 2rem; border-radius:14px;
      font-size:.92rem; font-weight:800;
      text-decoration:none; transition:all .25s;
      position:relative; overflow:hidden;
    }
    .btn-hero::before {
      content:''; position:absolute; inset:0;
      background:linear-gradient(90deg,transparent,rgba(255,255,255,.1),transparent);
      transform:translateX(-100%); transition:transform .5s;
    }
    .btn-hero:hover::before { transform:translateX(100%); }
    .btn-hero-primary {
      background:linear-gradient(135deg,var(--green),var(--green2));
      color:#fff; box-shadow:0 6px 24px rgba(16,185,129,.35);
    }
    .btn-hero-primary:hover { transform:translateY(-3px); box-shadow:0 10px 30px rgba(16,185,129,.45); }
    .btn-hero-secondary {
      background:rgba(255,255,255,.05); border:1.5px solid rgba(255,255,255,.12);
      color:var(--text);
    }
    .btn-hero-secondary:hover { border-color:var(--green); color:var(--green); }
    .hero-social-proof {
      display:flex; align-items:center; gap:1rem;
      animation:fadeUp .6s .4s ease both;
    }
    .avatars { display:flex; }
    .av {
      width:34px; height:34px; border-radius:50%;
      border:2px solid var(--dark); margin-left:-8px;
      object-fit:cover;
      background:linear-gradient(135deg,var(--green),#059669);
    }
    .av:first-child { margin-left:0; }
    .sp-text { font-size:.8rem; color:var(--soft); }
    .sp-text strong { color:var(--text); }
    .stars { color:#f59e0b; font-size:.9rem; letter-spacing:-.05em; }

    /* ── HERO RIGHT (image collage) ── */
    .hero-right {
      position:relative; height:100vh; overflow:hidden;
    }
    .hero-img-main {
      position:absolute; inset:0;
      background:url('https://images.unsplash.com/photo-1512621776951-a57141f2eefd?w=900&q=80') center/cover no-repeat;
    }
    .hero-img-main::after {
      content:'';
      position:absolute; inset:0;
      background:linear-gradient(to right, var(--dark) 0%, rgba(10,15,10,.4) 40%, transparent 100%);
    }
    .hero-float-card {
      position:absolute; z-index:10;
      background:rgba(20,31,20,.88); backdrop-filter:blur(16px);
      border:1px solid var(--border); border-radius:16px;
      padding:1rem 1.25rem;
      animation:floatCard 4s ease-in-out infinite;
    }
    @keyframes floatCard {
      0%,100%{transform:translateY(0);} 50%{transform:translateY(-8px);}
    }
    .fc-1 { bottom:20%; left:-40px; }
    .fc-2 { top:18%; right:5%; animation-delay:1.5s; }
    .fc-3 { bottom:38%; right:4%; animation-delay:3s; }
    .fc-label { font-size:.65rem; font-weight:700; color:var(--soft); text-transform:uppercase; letter-spacing:.07em; margin-bottom:.3rem; }
    .fc-val { font-family:'Fraunces',serif; font-size:1.4rem; font-weight:900; }
    .fc-sub { font-size:.7rem; color:var(--soft); margin-top:.1rem; }

    /* ── TRUST STRIP ── */
    .trust {
      position:relative; z-index:2;
      border-top:1px solid var(--border); border-bottom:1px solid var(--border);
      padding:1.5rem 2.5rem;
      display:flex; align-items:center; justify-content:center; gap:3rem; flex-wrap:wrap;
      background:rgba(16,185,129,.03);
    }
    .trust-item {
      display:flex; align-items:center; gap:.6rem;
      font-size:.82rem; font-weight:700; color:var(--soft);
    }
    .trust-item span:first-child { font-size:1.2rem; }

    /* ── SECTION SHARED ── */
    .section { position:relative; z-index:2; padding:6rem 5rem; }
    .section-eyebrow {
      display:inline-flex; align-items:center; gap:.4rem;
      background:rgba(16,185,129,.08); border:1px solid rgba(16,185,129,.18);
      border-radius:99px; padding:.3rem .85rem;
      font-size:.7rem; font-weight:800; color:var(--green);
      text-transform:uppercase; letter-spacing:.09em; margin-bottom:1.25rem;
    }
    .section-title {
      font-family:'Fraunces',serif;
      font-size:clamp(2rem,3.5vw,3rem); font-weight:900;
      line-height:1.1; margin-bottom:1rem;
    }
    .section-title em {
      font-style:italic;
      background:linear-gradient(135deg,var(--green),var(--green2));
      -webkit-background-clip:text; -webkit-text-fill-color:transparent;
    }
    .section-sub { font-size:1rem; color:var(--soft); line-height:1.7; max-width:540px; }

    /* ── RESULTS SECTION ── */
    .results-grid {
      display:grid; grid-template-columns:repeat(3,1fr); gap:1.5rem;
      margin-top:3rem;
    }
    .result-card {
      background:var(--card); border:1px solid var(--border);
      border-radius:20px; overflow:hidden;
      transition:all .3s; cursor:pointer;
    }
    .result-card:hover { border-color:rgba(16,185,129,.35); transform:translateY(-6px); }
    .result-img {
      height:220px; object-fit:cover; width:100%;
      transition:transform .4s;
    }
    .result-card:hover .result-img { transform:scale(1.04); }
    .result-body { padding:1.25rem 1.5rem; }
    .result-name { font-weight:800; font-size:.95rem; margin-bottom:.25rem; }
    .result-stat {
      font-family:'Fraunces',serif; font-size:1.5rem; font-weight:900;
      color:var(--green); margin-bottom:.25rem;
    }
    .result-desc { font-size:.78rem; color:var(--soft); line-height:1.5; }

    /* ── FEATURES ── */
    .features-layout {
      display:grid; grid-template-columns:1fr 1fr; gap:5rem; align-items:center;
      padding:6rem 5rem;
    }
    .features-img-wrap {
      position:relative; border-radius:24px; overflow:hidden;
    }
    .features-img {
      width:100%; height:520px; object-fit:cover; display:block;
    }
    .features-img-badge {
      position:absolute; bottom:1.5rem; left:1.5rem; right:1.5rem;
      background:rgba(10,15,10,.85); backdrop-filter:blur(16px);
      border:1px solid var(--border); border-radius:14px;
      padding:1.125rem 1.5rem;
      display:flex; align-items:center; gap:1rem;
    }
    .fib-icon {
      width:42px; height:42px; border-radius:11px; flex-shrink:0;
      background:linear-gradient(135deg,var(--green),var(--green2));
      display:flex; align-items:center; justify-content:center; font-size:1.15rem;
    }
    .fib-title { font-weight:800; font-size:.9rem; margin-bottom:.15rem; }
    .fib-sub { font-size:.72rem; color:var(--soft); }
    .features-list { display:flex; flex-direction:column; gap:1.25rem; }
    .feat-item {
      display:flex; gap:1rem; padding:1.25rem;
      background:var(--card); border:1px solid var(--border);
      border-radius:16px; transition:all .25s;
    }
    .feat-item:hover { border-color:rgba(16,185,129,.25); transform:translateX(6px); }
    .feat-icon {
      width:44px; height:44px; border-radius:12px; flex-shrink:0;
      display:flex; align-items:center; justify-content:center; font-size:1.2rem;
    }
    .feat-title { font-weight:800; font-size:.9rem; margin-bottom:.2rem; }
    .feat-desc { font-size:.78rem; color:var(--soft); line-height:1.5; }

    /* ── PLANS ── */
    .plans-section { padding:6rem 5rem; position:relative; z-index:2; }
    .plans-grid { display:grid; grid-template-columns:repeat(3,1fr); gap:1.25rem; margin-top:3.5rem; }
    .plan-card {
      background:var(--card); border:1.5px solid var(--border);
      border-radius:22px; padding:2rem;
      transition:all .3s; position:relative; overflow:hidden;
    }
    .plan-card::before {
      content:''; position:absolute; top:0; left:0; right:0; height:3px;
      opacity:0; transition:opacity .3s;
    }
    .plan-card:hover { transform:translateY(-8px); }
    .plan-card:hover::before { opacity:1; }
    .plan-card.featured {
      border-color:rgba(16,185,129,.4);
      background:linear-gradient(160deg,rgba(16,185,129,.07),var(--card));
    }
    .plan-card.featured::before { opacity:1; background:linear-gradient(90deg,var(--green),var(--green2)); }
    .plan-badge-pill {
      display:inline-flex; align-items:center; gap:.3rem;
      padding:.25rem .7rem; border-radius:99px;
      font-size:.68rem; font-weight:800; margin-bottom:1rem;
    }
    .plan-emoji { font-size:2.5rem; margin-bottom:.875rem; display:block; }
    .plan-name { font-family:'Fraunces',serif; font-size:1.3rem; font-weight:900; margin-bottom:.25rem; }
    .plan-price { font-family:'Fraunces',serif; font-size:2.8rem; font-weight:900; line-height:1; margin-bottom:.25rem; }
    .plan-period { font-size:.78rem; color:var(--soft); margin-bottom:1.5rem; }
    .plan-features { list-style:none; display:flex; flex-direction:column; gap:.6rem; margin-bottom:1.75rem; }
    .plan-features li {
      display:flex; align-items:center; gap:.6rem;
      font-size:.82rem; color:var(--soft);
    }
    .plan-features li::before {
      content:'✓'; color:var(--green); font-weight:900; font-size:.8rem; flex-shrink:0;
    }
    .plan-btn {
      display:block; width:100%; padding:.875rem;
      border-radius:12px; text-align:center; text-decoration:none;
      font-weight:800; font-size:.875rem; transition:all .22s;
      border:none; cursor:pointer;
    }
    .plan-btn-primary { background:linear-gradient(135deg,var(--green),var(--green2)); color:#fff; box-shadow:0 4px 16px rgba(16,185,129,.3); }
    .plan-btn-primary:hover { transform:translateY(-2px); box-shadow:0 8px 24px rgba(16,185,129,.4); }
    .plan-btn-secondary { background:rgba(255,255,255,.06); border:1.5px solid rgba(255,255,255,.12); color:var(--text); }
    .plan-btn-secondary:hover { border-color:var(--green); color:var(--green); }

    /* ── FOOD GALLERY ── */
    .gallery-section { position:relative; z-index:2; padding:0 5rem 6rem; }
    .gallery-title { font-family:'Fraunces',serif; font-size:2.5rem; font-weight:900; text-align:center; margin-bottom:.75rem; }
    .gallery-sub { text-align:center; color:var(--soft); margin-bottom:2.5rem; }
    .gallery-grid {
      display:grid;
      grid-template-columns:repeat(4,1fr);
      grid-template-rows:200px 200px;
      gap:1rem;
    }
    .gallery-item {
      border-radius:16px; overflow:hidden;
      position:relative; cursor:pointer;
    }
    .gallery-item:nth-child(1) { grid-column:span 2; grid-row:span 2; }
    .gallery-item img {
      width:100%; height:100%; object-fit:cover;
      transition:transform .4s;
    }
    .gallery-item:hover img { transform:scale(1.06); }
    .gallery-item::after {
      content:attr(data-label);
      position:absolute; bottom:0; left:0; right:0;
      padding:.6rem 1rem; background:linear-gradient(to top,rgba(0,0,0,.8),transparent);
      font-size:.78rem; font-weight:700; color:#fff;
      opacity:0; transition:opacity .3s;
    }
    .gallery-item:hover::after { opacity:1; }

    /* ── TESTIMONIALS ── */
    .testi-section { padding:6rem 5rem; position:relative; z-index:2; }
    .testi-grid { display:grid; grid-template-columns:repeat(3,1fr); gap:1.25rem; margin-top:3rem; }
    .testi-card {
      background:var(--card); border:1px solid var(--border);
      border-radius:20px; padding:1.75rem;
      transition:all .3s;
    }
    .testi-card:hover { border-color:rgba(16,185,129,.25); transform:translateY(-5px); }
    .testi-stars { color:var(--gold); font-size:.9rem; margin-bottom:1rem; }
    .testi-text { font-size:.875rem; color:var(--soft); line-height:1.7; margin-bottom:1.25rem; font-style:italic; }
    .testi-author { display:flex; align-items:center; gap:.75rem; }
    .testi-av {
      width:38px; height:38px; border-radius:50%;
      background:linear-gradient(135deg,var(--green),#059669);
      display:flex; align-items:center; justify-content:center;
      font-weight:800; font-size:.95rem; color:#fff; flex-shrink:0;
    }
    .testi-name { font-weight:800; font-size:.85rem; margin-bottom:.1rem; }
    .testi-meta { font-size:.72rem; color:var(--soft); }

    /* ── CTA SECTION ── */
    .cta-section {
      position:relative; z-index:2; margin:0 5rem 6rem;
      border-radius:28px; overflow:hidden;
      min-height:400px; display:flex; align-items:center;
    }
    .cta-bg {
      position:absolute; inset:0;
      background:url('https://images.unsplash.com/photo-1490645935967-10de6ba17061?w=1200&q=80') center/cover no-repeat;
    }
    .cta-overlay {
      position:absolute; inset:0;
      background:linear-gradient(135deg,rgba(10,15,10,.92),rgba(16,185,129,.15));
    }
    .cta-content {
      position:relative; z-index:1;
      padding:4rem 5rem; max-width:640px;
    }
    .cta-title { font-family:'Fraunces',serif; font-size:3rem; font-weight:900; line-height:1.1; margin-bottom:1rem; }
    .cta-title em { font-style:italic; color:var(--green); }
    .cta-sub { font-size:1rem; color:rgba(255,255,255,.7); line-height:1.7; margin-bottom:2.5rem; }
    .cta-btns { display:flex; gap:1rem; flex-wrap:wrap; }

    /* ── FOOTER ── */
    .footer {
      position:relative; z-index:2;
      border-top:1px solid var(--border);
      padding:3rem 5rem;
      display:flex; align-items:center; justify-content:space-between;
      flex-wrap:wrap; gap:1rem;
    }
    .footer-brand { font-family:'Fraunces',serif; font-weight:700; font-size:1rem; }
    .footer-brand span { color:var(--green); }
    .footer-links { display:flex; gap:2rem; }
    .footer-link { font-size:.8rem; color:var(--soft); text-decoration:none; transition:color .2s; }
    .footer-link:hover { color:var(--text); }
    .footer-copy { font-size:.75rem; color:var(--muted,#3a4a3a); }

    @keyframes fadeUp { from{opacity:0;transform:translateY(16px);}to{opacity:1;transform:translateY(0);} }

    @media(max-width:1024px) {
      .hero { grid-template-columns:1fr; }
      .hero-left { padding:8rem 2rem 4rem; }
      .hero-right { height:55vw; }
      .features-layout, .section { padding:4rem 2rem; }
      .plans-section,.testi-section,.gallery-section { padding:4rem 2rem; }
      .plans-grid,.testi-grid,.results-grid { grid-template-columns:1fr; }
      .gallery-grid { grid-template-columns:1fr 1fr; }
      .gallery-item:nth-child(1) { grid-column:span 1; grid-row:span 1; }
      .cta-section { margin:0 2rem 4rem; }
      .cta-content { padding:3rem 2rem; }
      .footer { padding:2rem; flex-direction:column; gap:1rem; }
    }
  </style>
</head>
<body>

<div class="bg-fixed"></div>
<div class="grain"></div>

<!-- ── NAV ── -->
<nav class="nav">
  <a href="/" class="nav-logo">🥑 Keto<span>Journey</span></a>
  <div class="nav-links">
    <a href="#results" class="nav-link">Results</a>
    <a href="#features" class="nav-link">Features</a>
    <a href="#plans" class="nav-link">Plans</a>
  </div>
  <div class="nav-cta">
    <a href="/login" class="btn-ghost">Log in</a>
    <a href="/signup" class="btn-green">Start Today →</a>
  </div>
</nav>

<!-- ── HERO ── -->
<section class="hero">
  <div class="hero-left">
    <div class="hero-eyebrow">🔥 Over 10,000 transformations</div>
    <h1 class="hero-title">
      Lose weight with<br/>
      <em>delicious</em> keto<br/>
      in <span class="gold">30 days</span>
    </h1>
    <p class="hero-sub">
      A complete keto system — personalized meal plans, 87 recipes, AI coaching, and daily accountability. No guesswork, just results.
    </p>
    <div class="hero-btns">
      <a href="/signup" class="btn-hero btn-hero-primary">
        <span>🚀</span> Start My Journey
      </a>
      <a href="#plans" class="btn-hero btn-hero-secondary">
        <span>💰</span> See Pricing
      </a>
    </div>
    <div class="hero-social-proof">
      <div class="avatars">
        <img class="av" src="https://images.unsplash.com/photo-1494790108755-2616b612b642?w=80&h=80&fit=crop&crop=face" alt="User"/>
        <img class="av" src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=80&h=80&fit=crop&crop=face" alt="User"/>
        <img class="av" src="https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=80&h=80&fit=crop&crop=face" alt="User"/>
        <img class="av" src="https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=80&h=80&fit=crop&crop=face" alt="User"/>
      </div>
      <div>
        <div class="stars">★★★★★</div>
        <div class="sp-text"><strong>4.9/5</strong> from 2,847 reviews</div>
      </div>
    </div>
  </div>

  <div class="hero-right">
    <div class="hero-img-main"></div>
    <div class="hero-float-card fc-1">
      <div class="fc-label">Average weight lost</div>
      <div class="fc-val" style="color:var(--green);">-14.7 kg</div>
      <div class="fc-sub">in the first 30 days</div>
    </div>
    <div class="hero-float-card fc-2">
      <div class="fc-label">Daily recipes</div>
      <div class="fc-val" style="color:var(--gold);">87 🍽️</div>
      <div class="fc-sub">all under 20g net carbs</div>
    </div>
    <div class="hero-float-card fc-3">
      <div class="fc-label">Streak record</div>
      <div class="fc-val" style="color:#f97316;">30 days 🔥</div>
      <div class="fc-sub">Sarah from Chicago</div>
    </div>
  </div>
</section>

<!-- ── TRUST ── -->
<div class="trust">
  <div class="trust-item"><span>🔒</span><span>Secure & Private</span></div>
  <div class="trust-item"><span>🛡️</span><span>30-Day Guarantee</span></div>
  <div class="trust-item"><span>📱</span><span>Works on Any Device</span></div>
  <div class="trust-item"><span>🤖</span><span>AI Keto Coach (Elite)</span></div>
  <div class="trust-item"><span>⚡</span><span>Instant Access</span></div>
</div>

<!-- ── RESULTS ── -->
<section class="section" id="results">
  <div class="section-eyebrow">✨ Real results</div>
  <h2 class="section-title">They did it.<br/>Now it's your <em>turn</em>.</h2>
  <p class="section-sub">Thousands of people just like you transformed their bodies — without giving up the foods they love.</p>
  <div class="results-grid">
    {[
      { img:'https://images.unsplash.com/photo-1571019613454-1cb2f99b2d8b?w=600&q=80', name:'Sarah M.', stat:'-18 kg', time:'in 30 days', desc:'I never thought I could enjoy eating healthy until Keto Journey. The recipes are incredible.' },
      { img:'https://images.unsplash.com/photo-1490645935967-10de6ba17061?w=600&q=80', name:'David R.', stat:'-22 kg', time:'in 45 days', desc:'The AI coach helped me stay on track even on my busiest days. Game changer.' },
      { img:'https://images.unsplash.com/photo-1498837167922-ddd27525d352?w=600&q=80', name:'Maria L.', stat:'-12 kg', time:'in 30 days', desc:'As a busy mom, the meal prep guides saved my life. Everything is so simple.' },
    ].map(r => (
      <div class="result-card">
        <img src={r.img} alt={r.name} class="result-img"/>
        <div class="result-body">
          <div class="result-name">{r.name}</div>
          <div class="result-stat">{r.stat} <span style="font-size:.9rem;color:var(--soft);font-family:'DM Sans'">{r.time}</span></div>
          <div class="result-desc">{r.desc}</div>
        </div>
      </div>
    ))}
  </div>
</section>

<!-- ── FOOD GALLERY ── -->
<section class="gallery-section" id="food">
  <div class="section-eyebrow" style="display:flex;justify-content:center;">🍳 87 keto recipes</div>
  <h2 class="gallery-title">Food that <em style="font-style:italic;background:linear-gradient(135deg,var(--green),var(--green2));-webkit-background-clip:text;-webkit-text-fill-color:transparent;">tastes</em> incredible</h2>
  <p class="gallery-sub">Every recipe is tested, delicious, and under 20g net carbs per serving.</p>
  <div class="gallery-grid">
    {[
      { img:'https://images.unsplash.com/photo-1546069901-ba9599a7e63c?w=800&q=80', label:'Avocado Power Bowl' },
      { img:'https://images.unsplash.com/photo-1567620905732-2d1ec7ab7445?w=400&q=80', label:'Keto Pancakes' },
      { img:'https://images.unsplash.com/photo-1546554137-f86b9593a222?w=400&q=80', label:'Grilled Salmon' },
      { img:'https://images.unsplash.com/photo-1565299624946-b28f40a0ae38?w=400&q=80', label:'Keto Pizza' },
      { img:'https://images.unsplash.com/photo-1476224203421-9ac39bcb3327?w=400&q=80', label:'Berry Smoothie' },
    ].map(item => (
      <div class="gallery-item" data-label={item.label}>
        <img src={item.img} alt={item.label} loading="lazy"/>
      </div>
    ))}
  </div>
</section>

<!-- ── FEATURES ── -->
<div class="features-layout" id="features">
  <div class="features-img-wrap">
    <img
      src="https://images.unsplash.com/photo-1490645935967-10de6ba17061?w=800&q=80"
      alt="Healthy keto food" class="features-img" loading="lazy"
    />
    <div class="features-img-badge">
      <div class="fib-icon">🤖</div>
      <div>
        <div class="fib-title">AI Keto Coach (Elite)</div>
        <div class="fib-sub">Answers any nutrition question, 24/7</div>
      </div>
    </div>
  </div>
  <div>
    <div class="section-eyebrow">⚡ Everything you need</div>
    <h2 class="section-title">Your complete<br/><em>keto system</em></h2>
    <p class="section-sub" style="margin-bottom:2rem;">Not just a recipe book — a full transformation program that guides you every single day.</p>
    <div class="features-list">
      {[
        { icon:'📅', g:'linear-gradient(135deg,#10b981,#059669)', title:'30-Day Structured Plan', desc:'Every meal planned for you. Just follow the day and stay on track.' },
        { icon:'📚', g:'linear-gradient(135deg,#3b82f6,#2563eb)', title:'87 Delicious Recipes + 12 eBooks', desc:'From breakfast to dessert — all tested, all under 20g carbs.' },
        { icon:'📊', g:'linear-gradient(135deg,#8b5cf6,#7c3aed)', title:'XP Progress Tracking', desc:'Earn points, unlock achievements, and watch your stats improve daily.' },
        { icon:'💧', g:'linear-gradient(135deg,#06b6d4,#0891b2)', title:'Smart Reminders', desc:'Meal, water, and streak reminders so you never fall off track.' },
        { icon:'🤖', g:'linear-gradient(135deg,#f59e0b,#d97706)', title:'AI Coach (Elite Only)', desc:'Get personalized advice, analyze meals, and stay motivated.' },
      ].map(f => (
        <div class="feat-item">
          <div class="feat-icon" style={`background:${f.g};box-shadow:0 4px 12px rgba(0,0,0,.2);`}>{f.icon}</div>
          <div>
            <div class="feat-title">{f.title}</div>
            <div class="feat-desc">{f.desc}</div>
          </div>
        </div>
      ))}
    </div>
  </div>
</div>

<!-- ── PLANS ── -->
<section class="plans-section" id="plans">
  <div style="text-align:center;">
    <div class="section-eyebrow" style="display:inline-flex;">💰 Simple pricing</div>
    <h2 class="section-title" style="margin-top:.5rem;">Pick your <em>plan</em></h2>
    <p class="section-sub" style="margin:0 auto .5rem;text-align:center;">One payment. Lifetime access within your plan period. 30-day guarantee.</p>
  </div>
  <div class="plans-grid">
    {[
      {
        emoji:'🥑', name:'Basic', price:'$29', period:'30 days',
        color:'#10b981', featured:false, badge:null,
        features:['30-day meal plan','30 keto recipes','4 eBooks','Progress tracking','Daily tasks & XP','Smart reminders'],
        link:'https://payhip.com/b/OEQk9', btnStyle:'secondary',
      },{
        emoji:'⚡', name:'Pro', price:'$99', period:'6 months',
        color:'#6366f1', featured:true, badge:'Most Popular',
        features:['6-month meal plan','87 keto recipes','8 eBooks','Advanced analytics','Priority support','Everything in Basic'],
        link:'https://payhip.com/b/OEQk9', btnStyle:'primary',
      },{
        emoji:'👑', name:'Elite', price:'$199', period:'12 months',
        color:'#f59e0b', featured:false, badge:'Best Value',
        features:['12-month meal plan','Unlimited recipes','12 eBooks','AI Keto Coach 🤖','24/7 premium support','Everything in Pro'],
        link:'https://payhip.com/b/OEQk9', btnStyle:'secondary',
      },
    ].map(plan => (
      <div class={`plan-card ${plan.featured ? 'featured' : ''}`}>
        {plan.badge && (
          <div class="plan-badge-pill" style={`background:${plan.color}18;border:1px solid ${plan.color}40;color:${plan.color};`}>
            ⭐ {plan.badge}
          </div>
        )}
        <span class="plan-emoji">{plan.emoji}</span>
        <div class="plan-name">{plan.name}</div>
        <div class="plan-price" style={`color:${plan.color};`}>{plan.price}</div>
        <div class="plan-period">{plan.period} access</div>
        <ul class="plan-features">
          {plan.features.map(f => <li>{f}</li>)}
        </ul>
        <a href={plan.link} class={`plan-btn ${plan.btnStyle === 'primary' ? 'plan-btn-primary' : 'plan-btn-secondary'}`}>
          Get {plan.name} Plan →
        </a>
      </div>
    ))}
  </div>
</section>

<!-- ── TESTIMONIALS ── -->
<section class="testi-section">
  <div style="text-align:center;">
    <div class="section-eyebrow" style="display:inline-flex;">💬 Real stories</div>
    <h2 class="section-title" style="margin-top:.5rem;">What people <em>say</em></h2>
  </div>
  <div class="testi-grid">
    {[
      { init:'S', name:'Sarah M.', meta:'Lost 18kg in 30 days', stars:5, text:'"I was skeptical at first, but this program completely changed my relationship with food. The recipes are genuinely delicious, and I never felt hungry."' },
      { init:'D', name:'David R.', meta:'Down 22kg in 45 days', stars:5, text:'"The AI coach is phenomenal. I can ask anything — portion sizes, substitutions, even what to order at restaurants. It\'s like having a nutritionist in my pocket."' },
      { init:'M', name:'Maria L.', meta:'Lost 12kg in 30 days', stars:5, text:'"As a mom of three, I barely have time to cook. The meal prep guides made everything so simple. My whole family loves the recipes now!"' },
      { init:'J', name:'James K.', meta:'Lost 15kg in 35 days', stars:5, text:'"The XP system kept me motivated every single day. I\'m competitive by nature, and hitting my daily goals became addictive in the best way."' },
      { init:'A', name:'Aisha T.', meta:'Lost 10kg in 30 days', stars:5, text:'"I\'ve tried so many diets. This is the first one where I actually felt satisfied after every meal. The keto recipes are creative and filling."' },
      { init:'R', name:'Robert C.', meta:'Lost 25kg in 60 days', stars:5, text:'"I expected basic stuff. Instead I got a complete system — tracking, recipes, AI help, reminders. It\'s the most comprehensive weight loss program I\'ve seen."' },
    ].map(t => (
      <div class="testi-card">
        <div class="testi-stars">{'★'.repeat(t.stars)}</div>
        <div class="testi-text">{t.text}</div>
        <div class="testi-author">
          <div class="testi-av">{t.init}</div>
          <div>
            <div class="testi-name">{t.name}</div>
            <div class="testi-meta">{t.meta}</div>
          </div>
        </div>
      </div>
    ))}
  </div>
</section>

<!-- ── CTA ── -->
<div class="cta-section">
  <div class="cta-bg"></div>
  <div class="cta-overlay"></div>
  <div class="cta-content">
    <h2 class="cta-title">Your <em>transformation</em><br/>starts today</h2>
    <p class="cta-sub">Join 10,000+ people who already changed their lives. 30-day money-back guarantee — zero risk.</p>
    <div class="cta-btns">
      <a href="/signup" class="btn-hero btn-hero-primary">🚀 Start My Journey</a>
      <a href="#plans" class="btn-hero btn-hero-secondary">View Plans</a>
    </div>
  </div>
</div>

<!-- ── FOOTER ── -->
<footer class="footer">
  <div class="footer-brand">🥑 Keto<span>Journey</span></div>
  <div class="footer-links">
    <a href="#" class="footer-link">Privacy</a>
    <a href="#" class="footer-link">Terms</a>
    <a href="#" class="footer-link">Contact</a>
    <a href="/login" class="footer-link">Login</a>
  </div>
  <div class="footer-copy">© 2025 Keto Journey. All rights reserved.</div>
</footer>

</body>
</html>